@model IEnumerable<GimnasioTitanes.Models.puesto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo Cargo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_puesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pue_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_puesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pue_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.id_puesto}) |
            @Html.ActionLink("Details", "Details", new { id = item.id_puesto }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id_puesto })
        </td>
    </tr>
}

</table>
