@model GimnasioTitanes.Models.proveedorSuplemento

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>proveedorSuplemento</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nom_proveedor)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nom_proveedor)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id_proveedorSuplemento }) |
    @Html.ActionLink("Back to List", "Index")
</p>
